SELECT DISTINCT u.CODUSUR,
       u.NOME,
       u.CODSUPERVISOR
  FROM PCUSUARI u
 WHERE u.CODSUPERVISOR NOT IN ('9999', '999999')
   AND NVL(u.BLOQUEIO, 'N') = 'N'
   /* PERMISSOES */
   AND (u.DTTERMINO IS NULL OR u.DTTERMINO > SYSDATE)
