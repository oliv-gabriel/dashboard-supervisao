SELECT m.CODUSUR,
       m.CODIGO AS CODFORNEC,
       SUM(NVL(m.VLVENDAPREV, 0)) AS VLMETA
  FROM PCMETA m
 WHERE m.TIPOMETA = 'F'
   AND m.DATA BETWEEN TRUNC(:DATAI, 'MM') AND LAST_DAY(:DATAF)
   AND m.CODFILIAL IN (/* FILIAIS */)
 GROUP BY m.CODUSUR, m.CODIGO